# Wan2GP Configuration Example
# Last Updated: 02/06/2026 12:00:00 PM CST
#
# Copy this file to the script directory as .env.wan2gp and customize as needed
# Location: /opt/divtools/scripts/wan2gp/.env.wan2gp

# Network settings
WAN2GP_SERVER_NAME=0.0.0.0  # 0.0.0.0 for LAN access, 127.0.0.1 for localhost only
WAN2GP_SERVER_PORT=7860

# ============================================================================
# Output Directories (where new downloaded files are saved)
# ============================================================================
# These are the default directories where NEW files will be downloaded to
WAN2GP_CHECKPOINTS_DIR=/opt/ai_models/sd_models/checkpoints
WAN2GP_DIFFUSION_MODELS_DIR=/opt/ai_models/sd_models/diffusion_models
WAN2GP_TEXT_ENCODERS_DIR=/opt/ai_models/sd_models/text_encoders
WAN2GP_UNET_DIR=/opt/ai_models/sd_models/unet
WAN2GP_LORAS_DIR=/opt/ai_models/sd_models/loras

# ============================================================================
# Additional Search Directories (where existing files are found)
# ============================================================================
# These directories are searched IN ADDITION to the output directories above.
# Use space or colon-separated paths. New files will NOT be saved here.
# These are useful for mounted NFS shares or external model libraries.

# Additional LoRAs directories (for finding existing LoRAs)
WAN2GP_ADDITIONAL_LORAS_DIRS=/opt/nfs_test/sd_models/loras

# Additional Checkpoints directories (for finding existing checkpoints)
WAN2GP_ADDITIONAL_CHECKPOINTS_DIRS=/opt/nfs_test/sd_models/checkpoints

# Additional Diffusion Models directories
WAN2GP_ADDITIONAL_DIFFUSION_MODELS_DIRS=/opt/nfs_test/sd_models/diffusion_models

# Additional Text Encoders directories
WAN2GP_ADDITIONAL_TEXT_ENCODERS_DIRS=/opt/nfs_test/sd_models/text_encoders

# Additional UNet directories
WAN2GP_ADDITIONAL_UNET_DIRS=/opt/nfs_test/sd_models/unet

# ============================================================================
# Multiple Directories Example
# ============================================================================
# To add multiple directories for searching, use space-separated or colon-separated paths:
# WAN2GP_ADDITIONAL_LORAS_DIRS="/path/to/loras1:/path/to/loras2:/path/to/loras3"
# OR
# WAN2GP_ADDITIONAL_LORAS_DIRS="/path/to/loras1 /path/to/loras2 /path/to/loras3"

# Additional arguments passed to wgp.py
# Examples: --gpu, --share, --fp16, --bf16, --advanced
#WAN2GP_EXTRA_ARGS="--gpu"
