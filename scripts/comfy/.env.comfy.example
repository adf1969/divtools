# ComfyUI Configuration Example
# Last Updated: 01/27/2026 11:00:00 AM CST
#
# Copy this file to your workspace as .env.comfy and customize as needed
# Location: /opt/comfy/.env.comfy

# Workspace path (where ComfyUI is installed)
COMFY_WORKSPACE=/opt/comfy

# Network settings
COMFY_LISTEN=0.0.0.0  # 0.0.0.0 for LAN access, 127.0.0.1 for localhost only
COMFY_PORT=8188

# Performance optimizations
# Enable SageAttention for ~20-40% speedup on video generation (requires installation)
#COMFY_USE_SAGE_ATTENTION=1

# Additional arguments passed to 'comfy launch'
# Examples: --gpu-only, --cpu-vae, --enable-manager, --preview-method auto
#COMFY_EXTRA_ARGS="--preview-method taesd"
COMFY_EXTRA_ARGS="--preview-method taesd --normalvram --cpu-vae-fallback --use-fp16-vae --disable-smart-memory --force-fp16"
