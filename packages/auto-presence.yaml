
# WARNING: This file is auto-generated by gen_presence_sensors.py
# Do not edit manually - changes will be overwritten!
# Generated: 2025-11-25 12:54:51

template:
  - binary_sensor:
      - name: Living Room Occupied
        unique_id: living_room_occupied_living_room
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Living Room')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('living_room')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Living Room')
    | list
    | count }}
          area_id: living_room
          area_slug: living_room
      - name: Kitchen Occupied
        unique_id: kitchen_occupied_kitchen
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Kitchen')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('kitchen')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Kitchen')
    | list
    | count }}
          area_id: kitchen
          area_slug: kitchen
      - name: Master Bedroom Occupied
        unique_id: master_bedroom_occupied_bedroom
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Master Bedroom')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('master_bedroom')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Master Bedroom')
    | list
    | count }}
          area_id: bedroom
          area_slug: master_bedroom
      - name: Bonus Room Occupied
        unique_id: bonus_room_occupied_bonus_room
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Bonus Room')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('bonus_room')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Bonus Room')
    | list
    | count }}
          area_id: bonus_room
          area_slug: bonus_room
      - name: Dining Room Occupied
        unique_id: dining_room_occupied_dining_room
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Dining Room')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('dining_room')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Dining Room')
    | list
    | count }}
          area_id: dining_room
          area_slug: dining_room
      - name: Office Occupied
        unique_id: office_occupied_office
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Office')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('office')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Office')
    | list
    | count }}
          area_id: office
          area_slug: office
      - name: Hobby Room Occupied
        unique_id: hobby_room_occupied_hobby_room
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Hobby Room')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('hobby_room')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Hobby Room')
    | list
    | count }}
          area_id: hobby_room
          area_slug: hobby_room
      - name: Shop Occupied
        unique_id: shop_occupied_shop
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Shop')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('shop')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Shop')
    | list
    | count }}
          area_id: shop
          area_slug: shop
      - name: Playroom Occupied
        unique_id: playroom_occupied_playroom
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Playroom')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('playroom')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Playroom')
    | list
    | count }}
          area_id: playroom
          area_slug: playroom
      - name: Dungeon Occupied
        unique_id: dungeon_occupied_dungeon
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Dungeon')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('dungeon')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Dungeon')
    | list
    | count }}
          area_id: dungeon
          area_slug: dungeon
      - name: Theater Occupied
        unique_id: theater_occupied_theater
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Theater')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('theater')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Theater')
    | list
    | count }}
          area_id: theater
          area_slug: theater
      - name: Garage Occupied
        unique_id: garage_occupied_garage
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Garage')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('garage')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Garage')
    | list
    | count }}
          area_id: garage
          area_slug: garage
      - name: Outside Occupied
        unique_id: outside_occupied_outside
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Outside')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('outside')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Outside')
    | list
    | count }}
          area_id: outside
          area_slug: outside
      - name: System Occupied
        unique_id: system_occupied_system
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'System')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('system')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'System')
    | list
    | count }}
          area_id: system
          area_slug: system
      - name: Christmas Occupied
        unique_id: christmas_occupied_christmas
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Christmas')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('christmas')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Christmas')
    | list
    | count }}
          area_id: christmas
          area_slug: christmas
      - name: Basement Living Room Occupied
        unique_id: basement_living_room_occupied_basement_living_room
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Basement Living Room')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('basement_living_room')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Basement Living Room')
    | list
    | count }}
          area_id: basement_living_room
          area_slug: basement_living_room
      - name: Basement Occupied
        unique_id: basement_occupied_basement
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Basement')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('basement')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Basement')
    | list
    | count }}
          area_id: basement
          area_slug: basement
      - name: Bedroom 2 Occupied
        unique_id: bedroom_2_occupied_bedroom_2
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Bedroom 2')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('bedroom_2')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Bedroom 2')
    | list
    | count }}
          area_id: bedroom_2
          area_slug: bedroom_2
      - name: Living Room 2 Occupied
        unique_id: living_room_2_occupied_living_room_2
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Living Room 2')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('living_room_2')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Living Room 2')
    | list
    | count }}
          area_id: living_room_2
          area_slug: living_room_2
      - name: Girls Rooms Occupied
        unique_id: girls_rooms_occupied_girls_rooms
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Girls Rooms')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('girls_rooms')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Girls Rooms')
    | list
    | count }}
          area_id: girls_rooms
          area_slug: girls_rooms
      - name: Thermostat Occupied
        unique_id: thermostat_occupied_thermostat
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Thermostat')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('thermostat')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Thermostat')
    | list
    | count }}
          area_id: thermostat
          area_slug: thermostat
      - name: Kids Room Occupied
        unique_id: kids_room_occupied_kids_room
        icon: mdi:account-group
        device_class: occupancy
        state: >
          {{ (expand('group.room_presence')
    | selectattr('state', 'eq', 'Kids Room')
    | list
    | count >= 1 )
    or
    (states.binary_sensor |
        selectattr('entity_id', 'in', area_entities('kids_room')) |
        rejectattr('attributes.device_class', 'undefined') |
        selectattr('attributes.device_class', 'search', '(occupancy|motion|door|running)') |
        selectattr('state', 'eq', 'on') |
        map(attribute='entity_id') |
        list | count > 0 )
}}
        attributes:
          people_count: >
            {{ expand('group.room_presence')
    | selectattr('state', 'eq', 'Kids Room')
    | list
    | count }}
          area_id: kids_room
          area_slug: kids_room