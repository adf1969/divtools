# Samba Active Directory Environment Variables
# Deploy Location: /home/divix/divtools/docker/sites/$SITE_NAME/$HOSTNAME/samba/.env.samba
# Example: /home/divix/divtools/docker/sites/s01-7692nw/ads1-98/samba/.env.samba
# Last Updated: 12/5/2025 8:00:00 AM CST
#
# IMPORTANT: This file contains sensitive passwords. Never commit to git.
# Create .env.samba.example without passwords for version control.

# Site and Host Information
SITE_NAME=s01-7692nw
SITE_NUM=1
HOSTNAME=ads1-98

# Domain Configuration
ADS_DOMAIN=avctn.lan
ADS_REALM=AVCTN.LAN
ADS_WORKGROUP=AVCTN

# Administrator Password (CHANGE THIS!)
# Must meet complexity requirements: uppercase, lowercase, numbers, special chars
ADS_ADMIN_PASSWORD=ChangeMe123!SecurePassword

# Network Configuration
ADS_HOST_IP=10.1.1.98
ADS_DNS_FORWARDER=8.8.8.8,8.8.4.4

# Server Role
# Options: dc (domain controller), member (member server)
ADS_SERVER_ROLE=dc

# Functional Level (optional)
# Options: 2008_R2, 2012, 2012_R2, 2016
ADS_DOMAIN_LEVEL=2016
ADS_FOREST_LEVEL=2016

# DNS Backend
# Options: SAMBA_INTERNAL, BIND9_DLZ, NONE
ADS_DNS_BACKEND=SAMBA_INTERNAL

# Logging
# Options: 0 (minimal) to 10 (debug)
ADS_LOG_LEVEL=1

# Optional: Existing domain join (for second DC)
# ADS_JOIN_EXISTING=false
# ADS_PRIMARY_DC=ads1-98.avctn.lan
